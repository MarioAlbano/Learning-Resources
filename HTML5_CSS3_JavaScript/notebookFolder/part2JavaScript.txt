//atribuições

var n = 3 
n = n + 4 
valor = 7

n += 4 // Significa n = valor de n + 4
n -= 5 // valor de n - 5
n *= 4 // valor de n vezes 4
n /= 2 // valor de n dividido por 2
n **=2 // valor de n elevado a 2
n %= 5 // resto da divisão de n por 5

n++ incrementa + 1 
n-- diminui - 1 


// Relacionais 

-Sempre retornam um valor booleano
>
<
>=
<=
==

! Negação		//Diz somente se algo é falso
&& e/conjunção 	//Duas combinações verdadeira para algo ser verdadeiro
|| ou/disjunção 	//Basta que um seja verdadeiro para ser verdadeiro
!=

Ordem de operações:
Primeiro operador aritmético () ** /
Segundo operadores relacionais	> < >= 
Terceiro operadores lógicos ! && ||

primeiro não
segundo e
terceiro ou

operador ternário ?:  //Faz uma entre duas ações disponíveis dependendo se for true ou false

*** DOM ****

Àrvore DOM = Document Object Model (Funciona quando roda dentro do navegador)

window = tudo dentro do JavaScript está no window

Dentro de window tem vários objetos. Ex:Location Document History
Dentro de document tem o html
em html, tem head e body

na div, colocar o "Disparar" do método
dentro do script, descrever a Function e seu nome

Função = Bloco de códigos que faz determinada tarefa
Método = Um objeto que tem uma função

Selecionando: 
-por Marca		//getElementsByagName()
-por ID			/getElementByid()
-por Nome		//getElementesByName()
-por Classe		//getElementsByClassName()
-por Seletor		//querySelector()   |   querySelectorAll()

Ex: 
var p1 = window.document.getElementsByTagName('p')[2] // A contagem sempre se inicia no 0
window.document.write(p1.innerText)
p1.style.color = 'blue'

var corpo = window.document.body
corpo.style.background = 'black'


** Evento DOM **

Ex de eventos:

mouseenter //Faz a ação quando estiver em hover
mousemove // Faz uma ação quando passar o mouse pelo elemento
mousedown // Faz uma ação quando estiver apertando o mouse
mouseup //   Faz uma ação quando soltar o botão do mouse
click//	     Faz uma ação ao clique	
mouseout// Faz uma ação ao tirar o mouse da tela

//procurar "javaScript Dom Event List" 

Funções

bloco {}
function = "Vai nomear oque está no bloco

Ex:
var a = window.document.getElementById("area");

function entrar() {
	a.innerText = "Entrou!"
	a.style.background = "red"
}


//Abaixo da variável

a.addEventListener("click", clicar);
a.addEventListener("mouseover", entrar);
a.addEventListener("mouseout", sair);

// Sempre que não executar seu código no JavaScript, use o chrome DevTools
