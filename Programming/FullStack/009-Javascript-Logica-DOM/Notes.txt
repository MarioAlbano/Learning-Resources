//O que vamos aprender nesse módulo?
-Juntar as peças combinando HTML, CSS e Javascript
-Criar comportamentos dinâmicos nas páginas web
-o que é DOM e como manipulá-lo
-Eventos
-2 mini projetos práticos

//O que são os eventos
-Forma dinâmica de executar o javascript numa página HTML
-Permite executar o javascript sem travar o carregamento da página
-Serão somente executadas se o usuário fazer ações específicas na página 

//Eventos
-onmouseenter
-onchange
-onfocus
-onblur	
-onclick
-como parâmetro, podemos criar uma nova função em um arquivo js externo e usar nos eventos

//DOM
-Modelo de objeto de documento
-É como se criasse um modelo da nossa página do documento em forma de objeto
-O javascript consegue acessar esse conteúdo HTML com poder de manipulálos como ler, criar, excluir, modificar elementos, modificar estilo

//Obtendo elementos HTML do DOM
-Obter vários elementos HTMLColletion: getElementsByTagName(), getElementByClassName()
-Obter vários elementos NodeList: getElementsByName(), querySelectorAll()
-obter um único elemento: getElementById(), querySelector()

//Usando na prática 
-criar uma variável = document.getElementById('nomeDoId')
-podemos utilizar o querySelector e querySelectorAll

//Criando novos elementos no DOM
-document.createElemente()
-.innerHTML adiciona conteúdo ao elemento
-.appendChild(newElement) adiciona um novo nó